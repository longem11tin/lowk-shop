import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  footer: {
    width: '100%',
    padding: '10px',
    height: '120px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '18px',
    backgroundImage: 'linear-gradient(to bottom right, #fafeff, #b5eaf3)',
  },
}));